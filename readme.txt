A simple News Website, that displays top 10 headlines from the BBC and SKY on a webpage. The feed is refreshed every 60s.

To install the dependencies execute the following command:

  npm install



To run the application, execute the following command:

  node index.js



Launch a browser and go to:

  http://localhost:8080/